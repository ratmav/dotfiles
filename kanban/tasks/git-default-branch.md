# 004: set git default branch to main

**milestone:** 1 - local experience

**dependencies:** none

## description

configure git to use `main` as the default branch name for new repositories instead of `master`.

## subtasks

- [ ] add `git config --global init.defaultbranch main` to bootstrap
- [ ] integrate into bootstrap git configuration
- [ ] test on fresh git init

## deliverable

new repos use main by default

## notes

none
